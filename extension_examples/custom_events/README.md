# llm-webchat-notifications

When multiple conversations are running at the same time and one finishes while
you're looking at a different conversation, a small ● dot briefly appears next
to its name in the sidebar.

## Installation and usage

From the repository root:

```bash
cd examples/notifications
uv pip install -e .
llm webchat-with-notifications
```
